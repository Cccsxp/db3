{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "Backend con json-server",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
